import { Card, PageTitle } from "@/components/ui"; import { merchants } from "@/lib/mock-data"; export default function Page({params}:{params:{id:string}}){const m=merchants.find(x=>x.id===params.id)||merchants[0]; return <><PageTitle title="AI 友好商家档案" desc="由问卷、文档和高德信息蒸馏出的结构化档案，可人工复核编辑。"/><div className="p-6"><Card><pre className="overflow-auto text-sm leading-6">{JSON.stringify(m.profile_json,null,2)}</pre></Card></div></>}
